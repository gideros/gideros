#include "platform.h"
#include <set>
#include <string>

/*
static double accelerometerx = 0;
static double accelerometery = 0;
static double accelerometerz = 0;

void setAccelerometer(double x, double y, double z)
{
	accelerometerx = x;
	accelerometery = y;
	accelerometerz = z;
}

void getAccelerometer(double* x, double* y, double* z)
{
	if (x)
		*x = accelerometerx;
	if (y)
		*y = accelerometery;
	if (z)
		*z = accelerometerz;
}
*/

/*
static PlatformInterface* platformInterface = 0;

void setPlatformInterface(PlatformInterface* platformInterface)
{
	::platformInterface = platformInterface;
}

void openUrl(const char* url)
{
	if (platformInterface != NULL)
		platformInterface->openUrl(url);
}
*/
